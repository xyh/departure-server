/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var StopRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return StopRouter;
});
