/*global define*/

define([
  'jquery',
  'underscore',
  'backbone',
  'text!templates/stops.html'
], function ($, _, Backbone, stopsTemplate) {
  'use strict';

  var StopsView = Backbone.View.extend({


  });

  return StopsView;
});
